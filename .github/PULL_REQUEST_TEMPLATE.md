<!-- Thanks for the PR. Keep it focused: one feature or fix. -->

## What this changes


## How I verified it
- [ ] `examples/*` still build (`make` in each, or `./forge play <example>`)
- [ ] `make test` prints `RENDER: PASS` for affected examples
- [ ] If the public API changed, `engine/engine.h` + `engine/engine.c` + `AGENTS.md` + `README.md` are in sync
- [ ] No build artifacts committed (`*.elf`, `*.o`, `shot.png`)
- [ ] No em dashes added
